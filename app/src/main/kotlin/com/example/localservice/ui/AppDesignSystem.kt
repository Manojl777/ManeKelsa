package com.example.localservice.ui

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.dp

/* ----------------------------------------------------------
   CATEGORY COLORS
---------------------------------------------------------- */

val ServiceBlue = Color(0xFF3B82F6)
val ServiceYellow = Color(0xFFEAB308)
val ServiceCyan = Color(0xFF06B6D4)
val ServicePurple = Color(0xFF8B5CF6)
val ServiceEmerald = Color(0xFF22C55E)
val ServiceOrange = Color(0xFFF97316)
val ServicePink = Color(0xFFEC4899)
val ServiceBrown = Color(0xFF8B4513)
val ServiceRed = Color(0xFFEF4444)

/* ----------------------------------------------------------
   PREMIUM EFFECT COLORS
---------------------------------------------------------- */

val GlassWhite = Color(0x99FFFFFF)
val SoftBorder = Color(0x14000000)

val OnlineGreen = Color(0xFF22C55E)
val OfflineRed = Color(0xFFEF4444)

val PremiumGold = Color(0xFFEAB308)

/* ----------------------------------------------------------
   SHAPES
---------------------------------------------------------- */

val LargeCardShape = RoundedCornerShape(32.dp)

val MediumCardShape = RoundedCornerShape(24.dp)

val SmallCardShape = RoundedCornerShape(18.dp)

val PillShape = RoundedCornerShape(100.dp)

val SquircleShape = RoundedCornerShape(40.dp)

val UploadShape = RoundedCornerShape(28.dp)

/* ----------------------------------------------------------
   SPACING
---------------------------------------------------------- */

val ScreenHorizontalPadding = 20.dp

val CardPadding = 18.dp

val SectionSpacing = 24.dp

/* ----------------------------------------------------------
   ELEVATIONS
---------------------------------------------------------- */

val LowElevation = 2.dp

val MediumElevation = 6.dp

val HighElevation = 12.dp